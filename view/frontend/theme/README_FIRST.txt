You can copy the theme files inside this directory into your own theme if you
want to override how a specific Train-Up! page looks.

For example:

Inside your active theme folder... if you create a file called tu_level.php,
then *that* will be used to render Training Levels, instead of single.php